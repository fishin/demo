{
  "name": "demo",
  "version": "0.0.1",
  "description": "demo ======",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/fishin/demo"
  },
  "author": "Lloyd Benson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fishin/demo/issues"
  },
  "homepage": "https://github.com/fishin/demo",
  "engines": {
    "node": ">=0.12.x"
  },
  "scripts": {
    "clean": "rm -rf node_modules",
    "console": "lab -a code -v -m 2000 -L",
    "test": "lab -a code -v -m 2000 -L -r json -o lab.json -r html -o lab.html -r console -o stdout -r console -o console.log"
  },
  "devDependencies": {
    "code": "1.x.x",
    "lab": "5.x.x"
  }
}
